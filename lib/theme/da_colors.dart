import 'package:flutter/material.dart';

class DAColors {
  DAColors._();

  // ── Primary greens ──────────────────────────────────────────────
  static const Color greenDark  = Color(0xFF1B5E20);
  static const Color greenMid   = Color(0xFF2E7D32);
  static const Color greenLight = Color(0xFF4CAF50);
  static const Color greenPale  = Color(0xFFE8F5E9);

  // ── Accent ──────────────────────────────────────────────────────
  static const Color amber      = Color(0xFFF57F17);
  static const Color amberLight = Color(0xFFFFB300);

  // ── Neutrals ────────────────────────────────────────────────────
  static const Color white     = Color(0xFFFFFFFF);
  static const Color offWhite  = Color(0xFFF5F5F5);
  static const Color textDark  = Color(0xFF1A1A1A);
  static const Color textMid   = Color(0xFF424242);
  static const Color textMuted = Color(0xFF757575);
  static const Color border    = Color(0xFFE0E0E0);

  // ── Status badges ───────────────────────────────────────────────
  static const Color draft    = Color(0xFF9E9E9E);
  static const Color unsync   = Color(0xFFFF9800);
  static const Color pending  = Color(0xFF2196F3);
  static const Color approved = Color(0xFF4CAF50);
}